name: distrobox
type: make
installCommand: make PREFIX=/usr install
sources:
  - type: git
    url: https://github.com/89luca89/distrobox.git
    tag: 2.0.0-rc.3
modules:
  - name: distrobox-deps-install
    type: apt
    sources:
      - packages:
          - golang-go
          - make
          - podman
  - name: distrobox-symlink
    type: shell
    commands:
      - mkdir -p /usr/share/apx/distrobox
      - ln -s /usr/bin/distrobox /usr/share/apx/distrobox/distrobox-export
      - ln -s /usr/bin/distrobox /usr/share/apx/distrobox/distrobox-host-exec
      - ln -s /usr/bin/distrobox /usr/share/apx/distrobox/distrobox-init
