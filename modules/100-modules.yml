name: modules
type: apt
source:
  packages:
  - dkms
  - bolt
  - inputattach
  - iucode-tool
  - cryptsetup-initramfs
  # remove temporarily due to build error
  # - acpi-call-dkms
  - libsasl2-modules
